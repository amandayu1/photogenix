{
    "scripts": {
        "emulate": "firebase emulators:start --import ./test-data --export-on-exit",
        "stripeHook": "stripe listen --forward-to http://localhost:5001/wellifyapp-staging/us-central1/paymentIntentResultHandler --events=payment_intent.succeeded",
        "firebase": "firebase",
        "postinstall": "cd functions && npm install"
    },
    "dependencies": {
        "@material-ui/core": "^4.11.3",
        "@material-ui/lab": "^4.0.0-alpha.57",
        "firebase-tools": "^9.5.0",
        "ics": "^2.27.0",
        "moment": "^2.29.1",
        "time-zone": "^1.0.0"
    }
}
